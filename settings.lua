data:extend({
  {
    type = "bool-setting",
    name = "nullius-early-transport-drones",
    setting_type = "startup",
    localised_name = "Early transport drones",
    localised_description = "Move the initial transport drone system from the robotics era to the traffic control era and replace the request depot's hangar requirement.",
    default_value = false
  }
})
